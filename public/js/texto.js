function copia(opt) {
  let elem = document.getElementById('txto')
  elem.innerHTML += `Me gustaría recibir información adicional sobre el plan ${opt} \nMi presupuesto es de: \nMi proyecto tiene las siguientes características:

  `
}